[DEFAULT]
# Host used by these Python scripts (running on the host) to reach Orion / Cygnus.
HOST_IP = localhost
# Host used by Orion (running inside its container) to notify Cygnus on the fiware-network.
CYGNUS_HOST = cygnus
