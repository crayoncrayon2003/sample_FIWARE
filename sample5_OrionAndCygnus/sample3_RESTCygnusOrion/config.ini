[DEFAULT]
# Host used by these Python scripts (running on the host) to reach Orion / Cygnus.
# Cygnus reaches the WebServer and Orion over the docker network via the static IPs in
# cygnus/config/agent.conf, so no extra host entries are needed here.
HOST_IP = localhost
