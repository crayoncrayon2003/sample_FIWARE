[DEFAULT]
# Host used by these Python scripts (running on the host) to reach Orion.
HOST_IP = localhost
# Host used by Draco (running inside its container) to reach Orion on the fiware-network.
ORION_HOST = orion1
# The sample WebServer (Step05) runs on the host, so Draco reaches it via the docker host gateway.
WEBSERVER_HOST = host.docker.internal
